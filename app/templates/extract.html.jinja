{% extends "base.html.jinja" %}
{% block content %}
    <h2>Pobierz opinie o produkcie:</h2>
    <form action="{{url_for('extract')}}" method="POST">
        <div class="row g-3 align-items-center">
            <div class="col-auto">
                <label for="product-id" class="col-form-label">kod produktu</label>
            </div>
            <div class="col-auto">
                <input type="text" id="produkt_id" name="product_id" class="form-control">
            </div>
            <div class="col-auto">
                <input type="submit" class="btn btn-pink" value="pobierz">
            </div>
        </div>
    </form>
{% endblock  %}